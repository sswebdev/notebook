<ul>
  <% @notes.each do |n| %>
    <%= render 'note', :note => n %>
  <% end %>
</ul>

<%= form_for Note.new, remote: true do |f| %>
      <%= f.text_field :content, placeholder: 'Add a new aha! here', size: 50 %>
    <% end %>
